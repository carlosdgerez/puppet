groups:
  - name: linux
    targets:
      - puppet.local
      - web.local
    config:
      transport: ssh
      ssh:
        user: vagrant
        run-as: root
        private-key: /puppet_course/bolt-ssh-key
config:
  ssh:
    host-key-check: false
    native-ssh: true
